# lab-utils-notify

Repo base para o lab de notificacao de releases do `container-utils`.

Este commit inicial contem apenas o esqueleto (Dockerfiles de exemplo +
briefing original). A proposta completa - workflow, documentacao de fluxo
e protocolo de validacao - esta sendo revisada no PR aberto contra `main`.

Consulte:

- `claude.md` - briefing original do desafio
- Dockerfiles de exemplo em `microservices-deployment-openjdk21/`,
  `python-deployment-3.12-slim/`, `react-deployment-nginx-with-envs/`

Apos merge do PR, este README sera substituido pela documentacao completa
do lab (guia para analista junior + troubleshooting + referencias).
